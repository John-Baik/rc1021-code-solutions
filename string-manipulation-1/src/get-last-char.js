/* exported getLastChar */
function getLastChar(string) {
  var lastIndex = string.length - 1;
  var lastChar = string[lastIndex];
  return lastChar;
}

// define function getLastChar with one parameter string
// define variable lastIndex that finds the last index of string
// define variable lastChar that finds the last character of string by using lastIndex variable
// return the lastChar
