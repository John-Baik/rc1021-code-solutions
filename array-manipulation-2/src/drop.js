/* exported drop */
function drop(array, count) {
  return array.slice(count);
}

// define function drop with 2 parameters, array and count
// slice array starting at count value and return array value
